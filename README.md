# Useful Scripts

This repo contains some useful scripts that can be used on any linux system with bash and python installed.

Note: Please create a `~/.env` file with the relevant variables for some scripts to function properly.
